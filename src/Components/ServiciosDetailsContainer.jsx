import React, {useState, useEffect} from "react"
import ServiciosDetails from "../Components/SericiosDetails"


export const ServiciosDetails = () => {
    const [data, setdata] = useState({});

    useEffect(() =>{
        const getData = new Promise((resolve) => {
           setTimeout(() => 
           resolve(film);
        },3000);
    });

     getData.then(res => setData(res));
};[]

    return{ 
       "<ServiciosDetails data=[data]/>"
    };


export default ServiciosDettailsContainer 